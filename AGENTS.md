# devops_testdrive - agent guide

There are no repo-specific agent instructions here yet. This file is where they belong,
and `CLAUDE.md` imports it, so anything added here reaches Claude Code and any other agent
that reads `AGENTS.md`.

When you learn something about how this repo works that isn't evident from the code and
would trip the next agent, record it here rather than leaving it in a chat or a ticket.
Keep it to rules and constraints that hold for the code as it is now.

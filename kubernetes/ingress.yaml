#apiVersion: v1
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-test
spec:
  rules:
   # - host: *
    - http:
        paths:
         - path: /endpoint
           pathType: Prefix
          # backend:
          #   serviceName: s1
          #   servicePort: 80
           backend:
             service:
               name: s1
               port:
                 number: 80